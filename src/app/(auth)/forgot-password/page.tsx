import ResetPasswordForm from "@/components/ResetPasswordForm";

export const metadata = { title: "Reset password — WealthLens" };

export default function ForgotPasswordPage() {
  return <ResetPasswordForm />;
}
